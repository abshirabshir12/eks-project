apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: default
  labels:
    app: 2048-game
spec:
  type: ClusterIP
  selector:
    app: 2048-game
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP